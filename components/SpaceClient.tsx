'use client';import {useState} from 'react';import type {Locale,Candidate} from '@/lib/types';import {usePortal} from './Providers';import {Header} from './Header';import {Logo} from './Logo';export function SpaceClient({locale}:{locale:Locale}){const {candidate,setCandidate}=usePortal();const [form,setForm]=useState<Candidate>({name:'',email:'',phone:'',city:'La Paz',role:'',consent:false});if(candidate)return <><Header locale={locale}/><main className="dashboard"><span className="eyebrow">BDP / Mi espacio</span><h1>Hola, {candidate.name}</h1><p className="lede">Tu perfil está listo para conectar con nuevas oportunidades.</p><div className="dash-grid"><div className="dash-card"><span>Perfil profesional</span><strong>80%</strong><div className="progress"><i/></div><button className="button button--dark">Completar perfil</button></div><div className="dash-card"><span>Convocatorias guardadas</span><strong>03</strong><p>Guarda oportunidades para volver cuando quieras.</p></div><div className="dash-card"><span>Bolsa de talento</span><strong>Activo</strong><p>Tu perfil puede ser considerado para futuras búsquedas.</p></div></div><button className="button button--line" onClick={()=>{setCandidate(null);localStorage.removeItem('bdp.candidate')}}>Cerrar sesión</button></main></>;return <><Header locale={locale}/><main className="auth-page"><div className="auth-page__intro"><Logo light/><span className="eyebrow">BDP / Mi espacio</span><h1>Construye tu siguiente oportunidad.</h1><p>Crea tu perfil una sola vez y mantén tu información lista para futuras convocatorias.</p></div><form className="glass-form" onSubmit={e=>{e.preventDefault();if(form.consent){setCandidate(form);localStorage.setItem('bdp.candidate',JSON.stringify(form))}}}><h2>Crear perfil</h2><label>Nombre completo<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Correo<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Teléfono<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Área de interés<input value={form.role} onChange={e=>setForm({...form,role:e.target.value})}/></label><label className="check"><input type="checkbox" checked={form.consent} onChange={e=>setForm({...form,consent:e.target.checked})}/> Acepto el uso de mis datos para procesos de talento.</label><button className="button button--bright">Crear perfil ↗</button></form></main></>}
