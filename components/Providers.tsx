'use client';import {createContext,useContext,useEffect,useMemo,useState,type ReactNode} from 'react';import type {Locale,Preference,Candidate} from '@/lib/types';import {copy} from '@/lib/i18n';
const Ctx=createContext<any>(null);export function getDictionary(locale:Locale){return copy}
export function Providers({children,locale}:{children:ReactNode,locale:Locale}){const [prefs,setPrefs]=useState<Preference>({largeText:false,contrast:false,daltonism:false,reducedMotion:false,tts:false});const [candidate,setCandidate]=useState<Candidate|null>(null);const [lang,setLang]=useState(locale);useEffect(()=>{const s=localStorage.getItem('bdp.prefs');if(s)setPrefs(JSON.parse(s));const c=localStorage.getItem('bdp.candidate');if(c)setCandidate(JSON.parse(c))},[]);useEffect(()=>{localStorage.setItem('bdp.prefs',JSON.stringify(prefs));document.documentElement.dataset.large=prefs.largeText?'true':'false';document.documentElement.dataset.contrast=prefs.contrast?'true':'false';document.documentElement.dataset.daltonism=prefs.daltonism?'true':'false';document.documentElement.dataset.reduced=prefs.reducedMotion?'true':'false'},[prefs]);const value=useMemo(()=>({locale:lang,setLang,prefs,setPrefs,candidate,setCandidate}),[lang,prefs,candidate]);return <Ctx.Provider value={value}>{children}</Ctx.Provider>}
export const usePortal=()=>useContext(Ctx);
