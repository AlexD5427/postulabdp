'use client';import {useEffect,useRef} from 'react';import Lenis from 'lenis';import {gsap} from 'gsap';import * as THREE from 'three';export function MotionLayer(){const host=useRef<HTMLDivElement>(null);useEffect(()=>{const lenis=new Lenis({duration:1.1,smoothWheel:true});const raf=(t:number)=>lenis.raf(t*1000);gsap.ticker.add(raf);const node=host.current;if(!node)return()=>{gsap.ticker.remove(raf);lenis.destroy()};const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(45,1,.1,100);camera.position.z=5;let renderer:THREE.WebGLRenderer;try{renderer=new THREE.WebGLRenderer({alpha:true,antialias:true})}catch{return()=>{gsap.ticker.remove(raf);lenis.destroy()}}renderer.setPixelRatio(Math.min(devicePixelRatio,1.5));node.appendChild(renderer.domElement);const geo=new THREE.IcosahedronGeometry(1.3,2);const mat=new THREE.MeshBasicMaterial({color:0x00b0d8,wireframe:true,transparent:true,opacity:.16});const mesh=new THREE.Mesh(geo,mat);scene.add(mesh);const resize=()=>{const w=node.clientWidth,h=node.clientHeight;renderer.setSize(w,h,false);camera.aspect=w/h;camera.updateProjectionMatrix()};resize();const ro=new ResizeObserver(resize);ro.observe(node);let frame=0;const tick=()=>{frame=requestAnimationFrame(tick);mesh.rotation.x+=.0015;mesh.rotation.y+=.0025;renderer.render(scene,camera)};tick();return()=>{cancelAnimationFrame(frame);ro.disconnect();geo.dispose();mat.dispose();renderer.dispose();if(renderer.domElement.parentNode===node)node.removeChild(renderer.domElement);gsap.ticker.remove(raf);lenis.destroy()}},[]);return <div ref={host} className="motion-art" aria-hidden="true"/>}
