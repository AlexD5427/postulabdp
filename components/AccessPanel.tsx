'use client';import {useState} from 'react';import {usePortal} from './Providers';export function AccessPanel(){const {prefs,setPrefs}=usePortal();const [open,setOpen]=useState(false);const speak=()=>{if(typeof speechSynthesis!=='undefined'){speechSynthesis.cancel();speechSynthesis.speak(new SpeechSynthesisUtterance('Portal de talento del Banco de Desarrollo Productivo'))}};return <><button className="access-trigger" onClick={()=>setOpen(!open)} aria-expanded={open}>Aa</button>{open&&<aside className="access-panel" aria-label="Accesibilidad"><strong>Accesibilidad</strong><button onClick={()=>setPrefs((p:any)=>({...p,largeText:!p.largeText}))}>Tamaño de texto</button><button onClick={()=>setPrefs((p:any)=>({...p,contrast:!p.contrast}))}>Alto contraste</button><button onClick={()=>setPrefs((p:any)=>({...p,daltonism:!p.daltonism}))}>Modo daltónico</button><button onClick={()=>setPrefs((p:any)=>({...p,reducedMotion:!p.reducedMotion}))}>Reducir movimiento</button><button onClick={speak}>Escuchar página</button></aside>}</>}
