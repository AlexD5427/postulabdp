import type {Metadata} from 'next';import type {ReactNode} from 'react';import {Inter,DM_Serif_Display} from 'next/font/google';import '@/styles/globals.css';import '@/styles/accessibility.css';
const inter=Inter({subsets:['latin'],variable:'--font-ui'});const serif=DM_Serif_Display({subsets:['latin'],weight:'400',variable:'--font-display'});export const metadata:Metadata={title:'Trabaja en BDP S.A.M. | Banco de Desarrollo Productivo',description:'Portal de talento del Banco de Desarrollo Productivo S.A.M.',icons:{icon:'/favicon.svg'}};export default function RootLayout({children}:{children:ReactNode}){return <html lang="es" className={`${inter.variable} ${serif.variable}`}><body>{children}</body></html>}
